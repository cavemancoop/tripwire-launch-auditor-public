node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# Orbio OAuth token store (AES-256-GCM at rest, but never commit it) + probe scratch
.orbio/
# M7: the worker's periodic benchmark.json snapshot — regenerated, not source
data/

# prisma generated client (regenerated by `pnpm db:generate`)
packages/db/src/generated/
**/.prisma/

# editor / os
.DS_Store
.idea/
.vscode/
*.log

# Word/Office lock files (created while a document is open)
~$*

/Tripwire commercial.docx
