[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# `forge script script/Deploy.s.sol --rpc-url robinhood --broadcast`
[rpc_endpoints]
robinhood = "${RH_RPC_URL}"
